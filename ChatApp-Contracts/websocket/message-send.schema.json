{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "MessageSendEvent",
  "description": "client → server. Submits a new message. id is client-generated and doubles as the dedup key through Kafka and into Postgres — see ChatApp-Service/DESIGN.md §5.",
  "type": "object",
  "properties": {
    "type": { "const": "message.send" },
    "id": { "type": "string", "format": "uuid" },
    "roomId": { "type": "string", "format": "uuid" },
    "body": { "type": "string", "minLength": 1 }
  },
  "required": ["type", "id", "roomId", "body"],
  "additionalProperties": false
}
