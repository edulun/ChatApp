{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "RoomJoinedEvent",
  "description": "server → client. Sent when a member joins a room (including the recipient's own join), so room list/membership stays in sync without a REST refetch.",
  "type": "object",
  "properties": {
    "type": { "const": "room.joined" },
    "room": { "$ref": "../domain/room.schema.json" },
    "userId": { "type": "string", "format": "uuid", "description": "The user who joined." }
  },
  "required": ["type", "room", "userId"],
  "additionalProperties": false
}
