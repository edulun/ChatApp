{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "TypingEvent",
  "description": "client → server. Signals the sender started or stopped typing in a room. Covers both typing.start and typing.stop, which share this shape.",
  "type": "object",
  "properties": {
    "type": { "type": "string", "enum": ["typing.start", "typing.stop"] },
    "roomId": { "type": "string", "format": "uuid" }
  },
  "required": ["type", "roomId"],
  "additionalProperties": false
}
