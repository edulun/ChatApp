# Loaded by `npm run dev:mock` (vite --mode mock). Not secret — safe to commit.
# Swaps RTK Query's baseQuery and the WebSocket middleware's socket for fixtures/mocks in
# src/mocks/, so the client runs without ChatApp-Service. See baseApi.ts, socketMiddleware.ts,
# App.tsx's mock sign-in button.
VITE_USE_MOCKS=true
